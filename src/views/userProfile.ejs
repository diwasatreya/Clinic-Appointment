<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.username %> – Profile | FindaCare</title>
    <link rel="shortcut icon" href="./images/favicon.ico" type="image/x-icon">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <script src="./js/tailwind.js"></script>
    <link rel="stylesheet" href="./css/style.css">
</head>
<body class="min-h-screen flex flex-col bg-page font-sans antialiased">

    <%- include('./particles/header') %>

    <div class="flex-grow max-w-2xl mx-auto w-full px-4 sm:px-6 lg:px-8 py-10">
        <header class="mb-8">
            <h1 class="text-3xl font-extrabold text-gray-900 flex items-center gap-3">
                <span class="flex h-12 w-12 items-center justify-center rounded-xl bg-accent-green/10 text-accent-green">
                    <i class="fa-solid fa-user text-xl"></i>
                </span>
                Profile
            </h1>
            <p class="text-gray-500 mt-1">Update your details for a better experience.</p>
        </header>

        <div class="card p-6 md:p-8">
            <h2 class="text-xl font-bold text-gray-900 mb-6">Personal information</h2>
            <form action="/auth/update" method="POST" class="space-y-6">
                <input type="hidden" name="oldphone" value="<%= dataUser.phone %>">
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                    <div>
                        <label for="first-name" class="block text-sm font-medium text-gray-700 mb-1">First name</label>
                        <input type="text" id="first-name" name="first-name" autocomplete="given-name" required value="<%= dataUser.firstName %>"
                            class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:ring-2 focus:ring-accent-green/40 focus:border-accent-green transition">
                    </div>
                    <div>
                        <label for="last-name" class="block text-sm font-medium text-gray-700 mb-1">Last name</label>
                        <input type="text" id="last-name" name="last-name" autocomplete="family-name" required value="<%= dataUser.lastName %>"
                            class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:ring-2 focus:ring-accent-green/40 focus:border-accent-green transition">
                    </div>
                </div>
                <div>
                    <label for="phone-number" class="block text-sm font-medium text-gray-700 mb-1">Phone</label>
                    <input type="tel" id="phone-number" name="phone-number" autocomplete="tel" required value="<%= dataUser.phone %>" placeholder="9812345678"
                        class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:ring-2 focus:ring-accent-green/40 focus:border-accent-green transition">
                </div>
                <div class="pt-4 border-t border-gray-100">
                    <h3 class="text-lg font-semibold text-gray-900 mb-3">Address</h3>
                    <label for="street-address" class="block text-sm font-medium text-gray-700 mb-1">Home address</label>
                    <input type="text" id="street-address" name="address" autocomplete="street-address" value="<%= dataUser.address || '' %>"
                        class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:ring-2 focus:ring-accent-green/40 focus:border-accent-green transition">
                </div>
                <div class="pt-4 flex justify-end">
                    <button type="submit" class="px-6 py-3 rounded-xl font-semibold text-white bg-accent-green hover:bg-emerald-600 transition shadow-soft btn-hover-scale">
                        Save changes
                    </button>
                </div>
            </form>
        </div>
    </div>

    <%- include('./particles/footer') %>
    <script src="./js/script.js"></script>
</body>
</html>
