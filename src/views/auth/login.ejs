<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - FindaCare</title>
    <link rel="shortcut icon" href="../images/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
        xintegrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />

    <!-- Tailwind CSS CDN and Professional Configuration -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="../js/tailwind.js"></script>
    <style>
        /* Ensures the content is vertically centered */
        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }

        main {
            flex-grow: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
        }
    </style>
</head>

<body class="bg-background-light font-sans text-text-dark">

    <main>
        <div class="w-full max-w-md bg-white p-8 sm:p-10 rounded-xl shadow-2xl border-t-8 border-accent-green">
            <div class="text-center mb-8">
                <i class="fa-solid fa-heart-pulse text-3xl text-accent-green mr-2"></i>
                <a href="/" class="text-3xl font-extrabold text-accent-green tracking-tight">
                    FindaCare
                </a>
                <h2 class="mt-4 text-2xl font-bold text-text-dark">
                    Sign in to your account
                </h2>
                <p class="mt-2 text-sm text-gray-600">
                    New user?
                    <a href="/auth/signup" class="font-medium text-primary-blue hover:text-primary-hover">
                        Create an account
                    </a>
                </p>
            </div>

            <% if (typeof error !== 'undefined' && error) { %>
            <div class="mb-4 p-3 bg-red-50 border border-red-200 rounded-lg">
                <p class="text-sm text-red-800 flex items-center">
                    <i class="fa-solid fa-circle-exclamation mr-2"></i>
                    <%= error %>
                </p>
            </div>
            <% } %>

            <% if (typeof success !== 'undefined' && success) { %>
            <div class="mb-4 p-3 bg-green-50 border border-green-200 rounded-lg">
                <p class="text-sm text-green-800 flex items-center">
                    <i class="fa-solid fa-circle-check mr-2"></i>
                    <%= success %>
                </p>
            </div>
            <% } %>

            <form class="space-y-6" action="/auth/login" method="post">
                <div>
                    <label for="phone" class="block text-sm font-medium text-gray-700 mb-1">Phone Number</label>
                    <input id="phone" name="phone" type="tel" autocomplete="tel" required
                        placeholder="1234567890" pattern="[0-9]{10}" maxlength="10" inputmode="numeric"
                        class="w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:ring-primary-blue focus:border-primary-blue transition duration-150">
                    <p class="mt-1 text-xs text-gray-500">Enter 10-digit phone number</p>
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
                    <input id="password" name="password" type="password" autocomplete="current-password" required
                        placeholder="••••••••"
                        class="w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:ring-primary-blue focus:border-primary-blue transition duration-150">
                </div>

                <div class="flex items-center justify-between">
                    <div class="flex items-center">
                        <input id="remember-me" name="rememberMe" type="checkbox"
                            class="h-4 w-4 text-accent-green border-gray-300 rounded">
                        <label for="remember-me" class="ml-2 block text-sm text-gray-900">Remember me</label>
                    </div>

                    <div class="text-sm">
                        <a href="#" class="font-medium text-primary-blue hover:text-primary-hover">
                            Forgot your password?
                        </a>
                    </div>
                </div>

                <div>
                    <button type="submit"
                        class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-md text-base font-semibold text-white bg-accent-green hover:bg-green-700 focus:outline-none focus:ring-4 focus:ring-primary-blue/50 transition duration-200">
                        Sign in
                    </button>
                </div>
            </form>
        </div>
    </main>

    <!-- Simple Footer -->
    <footer class="text-center text-xs text-gray-500 py-4 w-full">
        © 2025 FindaCare.
    </footer>
</body>

</html>