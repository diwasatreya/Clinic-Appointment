<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Appointments | Patient Portal</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'accent-green': '#10b981', // Your new primary color
                        'accent-light': '#e6fff5',  // Light background for green (Success/light primary)
                        'warning': '#f59e0b',       // Yellow/Amber
                        'success': '#10b981',       // Keeping 'success' the same as 'accent-green' for confirmed status
                    }
                }
            }
        }
    </script>
</head>
<body class="min-h-screen bg-gray-50 font-sans antialiased">

    <div class="max-w-4xl mx-auto p-4 sm:p-6 lg:p-8">
        
        <h1 class="text-3xl font-extrabold text-gray-900 sm:text-4xl mb-6 flex items-center">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-accent-green mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
            </svg>
            My Appointments
        </h1>

        <div class="border-b border-gray-200 mb-8">
            <nav class="-mb-px flex space-x-8" aria-label="Tabs">
                <a href="#" id="tab-upcoming" class="tab-button border-accent-green text-accent-green whitespace-nowrap py-3 px-1 border-b-2 font-semibold text-base" data-target="content-upcoming" aria-current="page">
                    Upcoming Appointments (2)
                </a>
                <a href="#" id="tab-past" class="tab-button border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300 whitespace-nowrap py-3 px-1 border-b-2 font-medium text-base" data-target="content-past">
                    Past Visits (8)
                </a>
            </nav>
        </div>
        
        <div class="appointment-content">

            <section id="content-upcoming" class="tab-content space-y-6">
                <h2 class="text-2xl font-bold text-gray-800">Upcoming</h2>

                <div class="bg-white shadow-lg rounded-xl p-5 border-l-4 border-accent-green transition duration-300 hover:shadow-xl">
                    <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center">
                        <div class="flex items-center space-x-4 mb-4 sm:mb-0">
                            <div class="text-center bg-accent-light p-3 rounded-lg flex-shrink-0">
                                <p class="text-xl font-bold text-accent-green">20</p>
                                <p class="text-xs text-gray-500 uppercase">Dec</p>
                            </div>
                            <div>
                                <p class="text-lg font-bold text-gray-900">Tuesday, 10:30 AM</p>
                                <p class="text-sm text-gray-600">Annual Check-up</p>
                            </div>
                        </div>
                        <span class="inline-flex items-center px-3 py-0.5 rounded-full text-sm font-medium bg-success/10 text-success ring-1 ring-inset ring-success/20">
                            Confirmed
                        </span>
                    </div>
                    
                    <hr class="my-4 border-gray-100">
                    
                    <div class="text-sm text-gray-700">
                        <div class="mb-4">
                            <p class="font-semibold text-gray-900">Doctor:</p>
                            <p>Dr. Sarah Chen, General Practitioner</p>
                        </div>
                        <div>
                            <p class="font-semibold text-gray-900">Reason for Visit:</p>
                            <p class="text-gray-600">This is a detailed annual physical exam appointment to review preventative care, vaccinations, and general health screenings. Please bring your insurance card and a list of all current medications.</p>
                        </div>
                    </div>
                    
                    <div class="mt-5 flex flex-wrap gap-3 justify-end">
                        <button class="cancel-btn px-3 py-2 text-sm font-medium text-white bg-red-600 rounded-lg hover:bg-red-700 transition shadow-md">
                            Cancel Appointment
                        </button>
                    </div>
                </div>

                <div class="bg-white shadow-lg rounded-xl p-5 border-l-4 border-warning transition duration-300 hover:shadow-xl">
                    <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center">
                        <div class="flex items-center space-x-4 mb-4 sm:mb-0">
                            <div class="text-center bg-yellow-50 p-3 rounded-lg flex-shrink-0">
                                <p class="text-xl font-bold text-warning">05</p>
                                <p class="text-xs text-gray-500 uppercase">Jan</p>
                            </div>
                            <div>
                                <p class="text-lg font-bold text-gray-900">Thursday, 2:00 PM</p>
                                <p class="text-sm text-gray-600">Follow-up Consultation</p>
                            </div>
                        </div>
                        <span class="inline-flex items-center px-3 py-0.5 rounded-full text-sm font-medium bg-warning/10 text-warning ring-1 ring-inset ring-warning/20">
                            Pending Confirmation
                        </span>
                    </div>
                    
                    <hr class="my-4 border-gray-100">
                    
                    <div class="text-sm text-gray-700">
                        <div class="mb-4">
                            <p class="font-semibold text-gray-900">Doctor:</p>
                            <p>Dr. David Lee, Cardiology</p>
                        </div>
                        <div>
                            <p class="font-semibold text-gray-900">Reason for Visit:</p>
                            <p class="text-gray-600">Follow-up visit to review results from recent stress test and ECG. We will discuss next steps for medication and lifestyle changes. Please ensure you fast for 12 hours prior to arrival.</p>
                        </div>
                    </div>
                    
                    <div class="mt-5 flex flex-wrap gap-3 justify-end">
                        <button class="cancel-btn px-3 py-2 text-sm font-medium text-white bg-red-600 rounded-lg hover:bg-red-700 transition shadow-md">
                            Cancel Appointment
                        </button>
                    </div>
                </div>
            </section>

            <hr class="my-10 border-gray-200">

            <section id="content-past" class="tab-content space-y-6 hidden">
                <h2 class="text-2xl font-bold text-gray-800">Past Visits</h2>
                
                <div class="relative mb-6">
                    <input type="text" id="past-visit-search" placeholder="Search past visits by doctor or reason..." 
                           class="w-full py-3 pl-10 pr-4 border border-gray-300 rounded-lg shadow-sm focus:ring-accent-green focus:border-accent-green transition">
                    <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                        <svg class="w-5 h-5 text-gray-400" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" /></svg>
                    </div>
                </div>

                <div class="bg-white shadow-sm rounded-lg p-4 border border-gray-200 hover:shadow-md transition duration-200 visit-card" data-doctor="Dr. Emily Roth" data-reason="Routine Check-up">
                    <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center">
                        <div class="flex items-center space-x-4 mb-3 sm:mb-0">
                            <div class="text-center bg-gray-100 p-2 rounded-md flex-shrink-0">
                                <p class="text-base font-bold text-gray-700">15</p>
                                <p class="text-xs text-gray-500 uppercase">Sept '25</p>
                            </div>
                            <div>
                                <p class="text-base font-semibold text-gray-900">Routine Check-up</p>
                                <p class="text-sm text-gray-600"><strong>Doctor:</strong> Dr. Emily Roth (Pediatrics)</p>
                            </div>
                        </div>
                        <div class="flex flex-wrap gap-3 items-center">
                            <span class="inline-flex items-center px-3 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-700">Completed</span>
                            <button class="view-summary-btn px-3 py-1.5 text-sm font-medium text-accent-green bg-accent-light rounded-lg hover:bg-accent-light/70 transition">
                                View Visit Summary
                            </button>
                        </div>
                    </div>
                </div>
                
                <div class="pt-4 text-center">
                    <a href="#" class="text-accent-green font-medium hover:text-success transition">View Full Appointment History →</a>
                </div>
            </section>
        </div>
    </div>
    
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const tabButtons = document.querySelectorAll('.tab-button');
            const tabContents = document.querySelectorAll('.tab-content');
            const searchInput = document.getElementById('past-visit-search');

            // --- 1. TAB SWITCHING LOGIC ---
            tabButtons.forEach(button => {
                button.addEventListener('click', (e) => {
                    e.preventDefault();
                    const targetId = e.currentTarget.getAttribute('data-target');
                    
                    // Reset and activate buttons logic
                    tabButtons.forEach(btn => {
                        btn.classList.remove('border-accent-green', 'text-accent-green', 'font-semibold');
                        btn.classList.add('border-transparent', 'text-gray-500', 'hover:text-gray-700', 'hover:border-gray-300', 'font-medium');
                    });
                    e.currentTarget.classList.add('border-accent-green', 'text-accent-green', 'font-semibold');
                    e.currentTarget.classList.remove('border-transparent', 'text-gray-500', 'hover:text-gray-700', 'hover:border-gray-300', 'font-medium');
                    
                    // Hide/Show content
                    tabContents.forEach(content => { content.classList.add('hidden'); });
                    document.getElementById(targetId).classList.remove('hidden');
                });
            });

            // --- 2. PAST VISIT SEARCH LOGIC ---
            if (searchInput) {
                searchInput.addEventListener('keyup', (e) => {
                    const searchTerm = e.target.value.toLowerCase();
                    const cards = document.querySelectorAll('.visit-card');
                    
                    cards.forEach(card => {
                        const doctorName = card.getAttribute('data-doctor').toLowerCase();
                        const reasonText = card.getAttribute('data-reason').toLowerCase();
                        
                        if (doctorName.includes(searchTerm) || reasonText.includes(searchTerm)) {
                            card.style.display = 'flex';
                        } else {
                            card.style.display = 'none';
                        }
                    });
                });
            }

            // --- 3. CANCEL BUTTON LOGIC ---
            document.querySelectorAll('.cancel-btn').forEach(button => {
                button.addEventListener('click', (e) => {
                    e.preventDefault();
                    if (confirm("Are you sure you want to cancel this appointment? This action cannot be undone.")) {
                        alert("Appointment has been canceled! (Placeholder action)");
                    }
                });
            });

            // --- 4. VIEW SUMMARY (Placeholder) ---
            document.querySelectorAll('.view-summary-btn').forEach(button => {
                button.addEventListener('click', (e) => {
                    alert("Navigating to detailed summary for this past visit...");
                });
            });
        });
    </script>

</body>
</html>