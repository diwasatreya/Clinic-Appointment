<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FindaCare – Find & Book Appointments</title>
    <link rel="shortcut icon" href="./images/favicon.ico" type="image/x-icon">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <script src="./js/tailwind.js"></script>
    <link rel="stylesheet" href="./css/style.css">
</head>
<body class="min-h-screen-flex bg-page font-sans antialiased">

    <%- include('./particles/header') %>

    <main class="flex-grow">
        <!-- Hero -->
        <section class="hero-gradient border-b border-emerald-100/50">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 md:py-20">
                <div class="text-center max-w-3xl mx-auto">
                    <h1 class="text-4xl md:text-5xl font-extrabold text-gray-900 tracking-tight mb-4">
                        Find the right care, <span class="text-accent-green">right now.</span>
                    </h1>
                    <p class="text-lg md:text-xl text-gray-600 mb-10">
                        Search by clinic name, specialty, or location and book your appointment in minutes.
                    </p>
                    <form action="/" method="get" class="flex flex-col sm:flex-row gap-3 max-w-2xl mx-auto">
                        <input type="text" name="search" placeholder="Clinic name, specialty, or location…"
                            class="flex-1 px-5 py-3.5 rounded-xl border border-gray-200 bg-white text-gray-800 placeholder-gray-400 focus:ring-2 focus:ring-accent-green/40 focus:border-accent-green shadow-soft transition">
                        <button type="submit" class="inline-flex items-center justify-center gap-2 px-6 py-3.5 rounded-xl font-semibold text-white bg-accent-green hover:bg-emerald-600 shadow-soft btn-hover-scale transition">
                            <i class="fa-solid fa-magnifying-glass"></i> Search
                        </button>
                    </form>
                </div>
            </div>
        </section>

        <!-- Clinics -->
        <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
            <h2 class="text-2xl font-bold text-gray-900 mb-6 flex items-center gap-2">
                <i class="fa-solid fa-building text-accent-green"></i>
                Available clinics
            </h2>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 clinic-list">
                <% if(typeof clinics !=='undefined' && clinics.length > 0) { %>
                    <% clinics.forEach((clinic)=> { %>
                    <article class="card card-hover flex flex-col h-full overflow-hidden">
                        <div class="p-6 flex flex-col flex-grow">
                            <div class="flex items-start gap-3 mb-4">
                                <span class="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl bg-accent-green/10 text-accent-green">
                                    <i class="fa-solid fa-hospital text-xl"></i>
                                </span>
                                <h3 class="text-lg font-bold text-gray-900 leading-tight">
                                    <%= clinic.clinicName %>
                                </h3>
                            </div>
                            <p class="text-gray-600 text-sm mb-4 line-clamp-3 flex-grow">
                                <%= clinic.description || 'No description available.' %>
                            </p>
                            <div class="space-y-2 text-sm">
                                <p class="flex items-center gap-2 text-gray-600">
                                    <i class="fa-solid fa-location-dot text-accent-green w-4"></i>
                                    <%= clinic.address || 'Downtown Area' %>
                                </p>
                                <p class="flex items-center gap-2 font-medium <%- (clinic.status == true && clinic.doctors.length != 0) ? 'text-accent-green' : 'text-red-500' %>">
                                    <i class="fa-solid <%- (clinic.status == true && clinic.doctors.length != 0) ? 'fa-circle-check' : 'fa-circle-xmark' %>"></i>
                                    <%- (clinic.status==true && clinic.doctors.length !=0) ? 'Accepting patients' : 'Not accepting' %>
                                </p>
                            </div>
                        </div>
                        <div class="p-6 pt-0">
                            <form action="/appoint" method="get">
                                <input type="hidden" name="id" value="<%= clinic._id.toString() %>">
                                <button type="submit" <%- (clinic.status==true && clinic.doctors.length !=0) ? '' : 'disabled' %>
                                    class="w-full py-3 rounded-xl font-semibold text-white transition shadow-soft
                                    <%- (clinic.status==true && clinic.doctors.length !=0)
                                        ? 'bg-accent-green hover:bg-emerald-600 btn-hover-scale' : 'bg-gray-400 cursor-not-allowed' %>">
                                    <i class="fa-solid fa-calendar-check mr-2"></i> Book appointment
                                </button>
                            </form>
                        </div>
                    </article>
                    <% }) %>
                <% } else { %>
                    <div class="sm:col-span-2 lg:col-span-4 flex flex-col items-center justify-center py-16 px-6 rounded-2xl border-2 border-dashed border-gray-200 bg-gray-50/50 text-center">
                        <span class="flex h-16 w-16 items-center justify-center rounded-2xl bg-gray-200 text-gray-500 mb-4">
                            <i class="fa-solid fa-magnifying-glass text-2xl"></i>
                        </span>
                        <p class="text-lg font-medium text-gray-700 mb-1">No clinics found</p>
                        <p class="text-gray-500 text-sm max-w-md">Try different keywords or location to find a clinic near you.</p>
                    </div>
                <% } %>
            </div>
        </section>
    </main>

    <%- include('./particles/footer') %>

    <script src="./js/script.js"></script>
</body>
</html>
